---
summary: "CLI reference for `aetherbot uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `aetherbot uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
aetherbot uninstall
aetherbot uninstall --all --yes
aetherbot uninstall --dry-run
```
