---
summary: "CLI reference for `aetherbot reset` (reset local state/config)"
read_when:
  - You want to wipe local state while keeping the CLI installed
  - You want a dry-run of what would be removed
title: "reset"
---

# `aetherbot reset`

Reset local config/state (keeps the CLI installed).

```bash
aetherbot reset
aetherbot reset --dry-run
aetherbot reset --scope config+creds+sessions --yes --non-interactive
```
