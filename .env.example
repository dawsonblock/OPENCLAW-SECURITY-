# OpenClaw .env example
#
# Quick start:
# 1) Copy this file to `.env` (for local runs from this repo), OR to `~/.openclaw/.env` (for launchd/systemd daemons).
# 2) Fill only the values you use.
# 3) Keep real secrets out of git.
#
# Env-source precedence for environment variables (highest -> lowest):
# process env, ./.env, ~/.openclaw/.env, then openclaw.json `env` block.
# Existing non-empty process env vars are not overridden by dotenv/config env loading.
# Note: direct config keys (for example `gateway.auth.token` or channel tokens in openclaw.json)
# are resolved separately from env loading and often take precedence over env fallbacks.

# -----------------------------------------------------------------------------
# Gateway auth + paths
# -----------------------------------------------------------------------------
# Recommended if the gateway binds beyond loopback.
OPENCLAW_GATEWAY_TOKEN=change-me-to-a-long-random-token
# Example generator: openssl rand -hex 32

# Optional alternative auth mode (use token OR password).
# OPENCLAW_GATEWAY_PASSWORD=change-me-to-a-strong-password

# Optional path overrides (defaults shown for reference).
# OPENCLAW_STATE_DIR=~/.openclaw
# OPENCLAW_CONFIG_PATH=~/.openclaw/openclaw.json
# OPENCLAW_HOME=~

# Optional: import missing keys from your login shell profile.
# OPENCLAW_LOAD_SHELL_ENV=1
# OPENCLAW_SHELL_ENV_TIMEOUT_MS=15000

# -----------------------------------------------------------------------------
# Model provider API keys (set at least one)
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=...
# OPENROUTER_API_KEY=sk-or-...

# Optional additional providers
# ZAI_API_KEY=...
# AI_GATEWAY_API_KEY=...
# MINIMAX_API_KEY=...
# SYNTHETIC_API_KEY=...

# -----------------------------------------------------------------------------
# Channels (only set what you enable)
# -----------------------------------------------------------------------------
# TELEGRAM_BOT_TOKEN=123456:ABCDEF...
# DISCORD_BOT_TOKEN=...
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...

# Optional channel env fallbacks
# MATTERMOST_BOT_TOKEN=...
# MATTERMOST_URL=https://chat.example.com
# ZALO_BOT_TOKEN=...
# OPENCLAW_TWITCH_ACCESS_TOKEN=oauth:...
# OPENCLAW_FEISHU_WEBHOOK_ALLOW_LAN=0
# OPENCLAW_MSTEAMS_WEBHOOK_ALLOW_LAN=0

# -----------------------------------------------------------------------------
# Tools + voice/media (optional)
# -----------------------------------------------------------------------------
# BRAVE_API_KEY=...
# PERPLEXITY_API_KEY=pplx-...
# FIRECRAWL_API_KEY=...

# ELEVENLABS_API_KEY=...
# XI_API_KEY=...  # alias for ElevenLabs
# DEEPGRAM_API_KEY=...

# -----------------------------------------------------------------------------
# RFSN kernel policy (production profile)
# -----------------------------------------------------------------------------
# Keep RFSN in allowlist mode.
# OPENCLAW_RFSN_MODE=allowlist

# Emergency-only escape hatch. Leave unset/0 in production.
# OPENCLAW_RFSN_AUTOWHITELIST_ALL_TOOLS=0

# Deny-by-default tool allowlist for embedded agent and gateway invoke flows.
# Keep this in sync with your approved operational toolset.
# OPENCLAW_RFSN_ALLOW_TOOLS=agents_list,apply_patch,browser,canvas,cron,edit,exec,gateway,image,memory_get,memory_search,message,nodes,process,read,session_status,sessions_history,sessions_list,sessions_send,sessions_spawn,tts,web_fetch,web_search,write

# Optional emergency deny list (always wins).
# OPENCLAW_RFSN_DENY_TOOLS=

# Capability grants (explicitly enable only what your deployment needs).
# - fs:* gates file reads/writes
# - proc:* gates exec/process
# - net:* gates outbound/networked tools
# - server:* gates local listeners/tools that expose surfaces
# Note: `proc:manage` should only be granted when tools run in a sandbox.
# OPENCLAW_RFSN_GRANTED_CAPABILITIES=fs:read:workspace,fs:write:workspace,proc:manage,session:spawn,net:outbound,net:search,net:tts,net:messaging,net:gateway,net:browser,server:local:canvas

# Exec safe bins used by RFSN exec gate. Commands outside this set are denied.
# Keep this list short and review additions.
# OPENCLAW_RFSN_EXEC_SAFE_BINS=cat,cp,cut,echo,find,git,grep,head,jq,ls,mkdir,mv,pwd,rg,sed,sort,tail,touch,tr,uniq,wc,which

# Outbound domain allowlist enforced by RFSN for URL-based tools.
# Add only trusted destinations your agents are expected to use.
# OPENCLAW_RFSN_FETCH_ALLOW_DOMAINS=docs.openclaw.ai,api.github.com,github.com,raw.githubusercontent.com,en.wikipedia.org,wikipedia.org

# Domain policy toggles.
# OPENCLAW_RFSN_FETCH_ALLOW_SUBDOMAINS=1
# OPENCLAW_RFSN_ENFORCE_FETCH_DOMAIN_ALLOWLIST=1

# Exec command hardening.
# OPENCLAW_RFSN_BLOCK_EXEC_COMMAND_SUBSTITUTION=1
